// 项目默认配置

const config = {
	port:3000,			//默认启动端口
	
	//数据库配置
	database:{
		DATABASE: 'nodesql',
	    USERNAME: 'root',
	    PASSWORD: '123456',
	    PORT: '3306',
	    HOST: 'localhost'
	}
}

module.exports = config;
